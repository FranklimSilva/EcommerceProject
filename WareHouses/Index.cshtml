@model IEnumerable<ECommerceProject.Models.WareHouse>

@{
    ViewBag.Title = "Index";
}

    <h2>Armazem</h2>

<p>
    @Html.ActionLink("Criar Armazem", "Create", new { }, new {@class="btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cities.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Companies.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Departments.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cities.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Companies.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Departments.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.WareHouseId }, new {@class="btn btn-warning" }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.WareHouseId }, new {@class="btn btn-info" }) |
            @Html.ActionLink("excluir", "Delete", new { id=item.WareHouseId }, new {@class="btn btn-danger" })
        </td>
    </tr>
}

</table>
